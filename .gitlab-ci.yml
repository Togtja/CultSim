image: frolvlad/alpine-gxx

# Install dependencies for build up front
before_script:
  - apk update
  - apk add git make cmake mesa mesa-dev lua libx11-dev libxext-dev

test:
  tags:
    - docker
  script:
    - cmake --version
    - mkdir build
    - cd build
    - cmake .. -G "Unix Makefiles"
    - make -j2
    - ctest

build:
  tags:
    - docker
  script:
    - echo "CMake Build"
